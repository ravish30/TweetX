import { createTheme } from '@mui/material/styles'

const theme = createTheme({
    palette: {
        primary: {
            main: "rgba(0,0,0, 0.7)"
        },
        secondary: {
            main: 'rgba(239, 68, 68, 0.75)'
        }
    }
})



export default theme;